//Challenge 1: Stack vs. Queue
//Explain the difference between a stack and a queue. Provide two real-life examples for each data structure.
//스택(Stack)과 큐(Queue)의 차이점을 설명한다. 각 자료 구조(Data Structure)에  대한 두 가지 실제 예제를 제시한다.




//Queue는 선입선출(first-in-first-out) 이다. 먼저 삽입된 것이 반드시 먼저 제거된다. 먼저 삽입된 것이 반드시 먼저 제거된다.
//Queue는 후면에서 삽입되고, 전면에서 제거된다.
//ex.
//  1. 영화관에서 줄 서기 : 티켓 구입 시 줄을 서서 순서대로 구매한다.
//  2. 프린터 : 먼저 작업 queue에 올라간 파일부터 순차적으로 인쇄된다.

//Stack은 후입선출(last-in-first-out) 나중에 삽입된 것이 반드시 먼저 제거된다.
//Stack은 상단에서 삽입되고, 상단에서 제거된다.
//ex.
//  1. 쌓인 접시 : 접시를 위로 차근차근 쌓으며, 사용할 때는 위에서 부터 순서대로 꺼낸다.
//  2. 취소 기능 : 컴퓨터의 많은 프로그램에서, Ctrl-Z를 사용하면 가장 최근 작업이 취소된다.
